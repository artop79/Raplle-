Raplle.com
Задача проекта: Автоматизировать начатьный этап найма кандидатов с помощью AI-технологий анализа резюме и интервью кандидата
AI- анализ резюме кандидата, интервью кандидата, формирование отчета по итогам собеседования

# AI анализ резюме кандидата:

## 🧠 Общая логика работы

1. AI анализирует резюме кандидата и сравнивает его с требованиями вакансии и определят в процентах насколько кандидат подходит к вакансии и формирует отчет
2. Ai определяет какой из кандидатов на работу больше всего подходит к вакансии (если их несколько) и предлагает записаться в zoom встречу
3. Тем кто не подходит требованиям вакансии просит ждать ответа от HR и после того как на должность выбран кандидат автоматически через @mail отправляет сообщение кандидату что на данную должность уже найден кандидат 
4. ИИ автоматически передает данные ИИ интервьюеру который проводит собеседокание с кандидатом и исходя от результатов аналища резюме задает вопросы кандидату 

1. История кандидата и отчёты для HR-отдела
🔹 Добавь возможность формировать PDF/табличные отчеты по каждому кандидату (резюме, % совпадения, вопросы, ответы и т.п.)
📂 Это нужно для внутреннего хранения, согласования с руководством, а также для юридических целей.

2. Контроль качества ИИ-интервью (Human-in-the-loop)
🔹 Возможность для HR вручную просматривать результаты интервью, редактировать вопросы, и корректировать финальные рекомендации.
📌 Это повысит доверие и снизит риски.

3. Интеграция с ATS (или Google Sheets на старте)
🔹 Добавь хотя бы простую интеграцию (CSV или Google Sheets), чтобы передавать туда:
– резюме,
– интервью-результаты,
– рекомендации,
– финальный статус.

4. Календарная интеграция для Zoom (Google Calendar или Outlook)
🔹 AI должен не просто предлагать Zoom встречу, но и создавать её автоматически + отправлять инвайты (кандидату и HR).

5. Поддержка нескольких вакансий
🔹 Один и тот же работодатель может загружать несколько вакансий. Нужно предусмотреть работу с каждой вакансией отдельно:
– автоматическое распределение кандидатов,
– аналитика по каждой вакансии.


#  AI-Интервьюер 
Эта платформа преднозначена для HR агентов сейчас 
нам нужно разработать Ai интервьюэра который будет
 проводить видео собеседование с кандидатами здесь 
 мы будем использовать heygen, а так-же ИИ интервьюер
  это 2 этап после анализа резюме в интерфейсе Ai интервьюэра
   должен быть список кандидатов резюме которых мы проверили
    и HR менеджер выбирает с кеб будет проводиться интервью а с кем
     нет  тоесть проверка резюме и Ai интервьюэр связаны и hr менеджер
      может смотреть результаты анализов в резюме и оценивать кому
       назначить встречу с ИИ интервьюэром 
## 🧠 Общая логика работы

1. HR создаёт интервью (задаёт вакансию + список вопросов).
2. Кандидат получает ссылку на Zoom с ботом-интервьюером.
3. AI участвует в Zoom, как участник с видео и голосом (аватар).
4. AI задаёт заранее подготовленные и/или сгенерированные вопросы.
5. AI **слушает ответы** (реальное время или после интервью).
6. AI оценивает ответы по заданным критериям (soft/hard skills).
7. AI формирует **отчёт**: PDF/таблица + видео/текст анализа.

---

## 🔧 Функциональные блоки

### 🔹 1. Генерация вопросов

* GPT-4 генерирует список вопросов на основе описания вакансии.
* Категории: опыт, навыки, кейсы, soft skills, ситуационные вопросы.
* Возможность ручной правки HR-менеджером.
### 🔹 2. Интеграция с Zoom

* Создание Zoom-встречи через API
* Бот подключается как участник с виртуальной камерой (с аватаром)
* Используем [Zoom SDK + Zoom Bot](https://marketplace.zoom.us/docs/guides/bot/)
* Видеоаватар генерируется с помощью **Synthesia / D-ID / DeepBrain**

### 🔹 3. Синтез речи

* AI говорит голосом — TTS (Text-to-Speech)
* Язык: английский (с возможностью расширения)
* Технологии: ElevenLabs / Azure TTS / Google TTS

### 🔹 4. Аудио-распознавание

* Запись и транскрипция речи кандидата
* Используем:

  * [Whisper (OpenAI)](https://openai.com/research/whisper) или
  * Google Speech-to-Text
* Мультиязычная поддержка (начать с английского)

### 🔹 5. Анализ ответов

* Оценка по:

  * Ясности мышления
  * Уровню навыков (на основе ключевых фраз)
  * Уверенности
  * Соответствию профилю вакансии
* AI даёт вывод: Strong / Moderate / Weak fit + аргументы

### 🔹 6. Финальный отчет

* PDF или HTML-документ с:

  * Резюме интервью
  * Баллами по категориям
  * Цитатами из ответов
  * Выводом: рекомендовать / не рекомендовать

---

## ⚙️ Технологии

| Задача               | Инструмент                             |
| -------------------- | -------------------------------------- |
| Видеогенерация       | D-ID API / DeepBrain / Synthesia       |
| Голос                | ElevenLabs / Azure TTS / Google TTS    |
| Распознавание речи   | OpenAI Whisper / Google STT            |
| Анализ речи / ответы | GPT-4 + собственная логика скоринга    |
| Zoom-интеграция      | Zoom SDK + Bot OAuth API               |
| Бэкенд               | Python (FastAPI) / Node.js             |
| Хранение и отчёты    | Supabase / MongoDB / Firebase + PDFKit |

---

## 📦 Выходные данные

* Страница с результатами
* Отчёт в PDF
* Видео или расшифровка интервью
* Скоринг по ключевым метрикам



# **Техническая реализация AI HR Agent с быстрой интеграцией**

## **1. Общая архитектура решения**
AI HR Agent будет реализован как микросервис с REST API и готовыми интеграциями для популярных платформ. Основные компоненты:

```
Frontend (React) ↔ Backend (FastAPI/Node.js) ↔ AI HR Agent Core
       ↑
Интеграции (Telegram, Slack, LinkedIn, Email, Zoom, Calendars)
```

## **2. Автоматическая коммуникация с быстрой интеграцией**

### **Telegram Integration**
**Технологии:**
- Telegram Bot API
- Python `python-telegram-bot` или Node.js `telegraf`

**Реализация:**
1. Клиент вводит API токен своего бота в админке
2. Наш сервис подключается к боту через webhook
3. Бот начинает автоматически:
   - Отправлять приглашения кандидатам
   - Получать ответы
   - Обновлять статусы в системе

```python
# Пример быстрого подключения
from telegram import Update
from telegram.ext import ApplicationBuilder, CommandHandler

async def start(update: Update, context):
    await update.message.reply_text('Бот HR Agent активирован!')

app = ApplicationBuilder().token("CLIENT_PROVIDED_TOKEN").build()
app.add_handler(CommandHandler("start", start))
app.run_polling()
```

### **Slack Integration**
**Технологии:**
- Slack Bolt API
- OAuth 2.0 для быстрого подключения

**Реализация:**
1. Клиент авторизуется через OAuth в нашем приложении
2. Мы получаем токен доступа
3. Автоматически создаем:
   - Канал для уведомлений
   - Бота для коммуникации
   - Webhook для событий

```javascript
// Пример с Slack Bolt
const { App } = require('@slack/bolt');

const app = new App({
  token: process.env.SLACK_BOT_TOKEN,
  signingSecret: process.env.SLACK_SIGNING_SECRET
});

app.event('app_mention', async ({ event, say }) => {
  await say(`Привет! Я ваш HR Agent. Готов к работе!`);
});
```

### **LinkedIn Integration (ограниченная)**
**Технологии:**
- LinkedIn Marketing API (для сообщений)
- Альтернатива: Selenium/Puppeteer для автоматизации

**Реализация:**
1. Клиент предоставляет cookies или API ключ
2. Настраиваем автоматическую отправку сообщений
3. Ограничение: 100 сообщений/месяц на аккаунт

### **Email (SendGrid)**
**Технологии:**
- SendGrid API v3
- Шаблоны с динамическими переменными

**Реализация:**
```python
from sendgrid import SendGridAPIClient
from sendgrid.helpers.mail import Mail

message = Mail(
    from_email='hr@company.com',
    to_emails='candidate@email.com',
    subject='Интервью в {{company_name}}',
    html_content='<strong>Привет, {{name}}!</strong>')
try:
    sg = SendGridAPIClient(os.environ.get('SENDGRID_API_KEY'))
    response = sg.send(message)
except Exception as e:
    print(e.message)
```

## **3. Шаблоны отказов с персонализацией**
**Реализация:**
- Хранение шаблонов в JSON/DB
- Подстановка переменных:
  - `{{candidate_name}}`
  - `{{position}}`
  - `{{reason}}`
  - `{{company}}`

**Пример шаблона:**
```json
{
  "template_id": "rejection_1",
  "subject": "Отклик на вакансию {{position}}",
  "body": "Уважаемый {{candidate_name}}, к сожалению...",
  "channels": ["email", "telegram"]
}
```

## **4. Проверка резюме**
**Технологии:**
- Apache Tika (для извлечения текста)
- SpaCy/NLTK (для NLP)
- Custom ML модели для классификации

**Процесс:**
1. Загрузка файла (PDF/DOCX/TXT)
2. Извлечение текста
3. Анализ:
   - Контакты
   - Опыт работы
   - Навыки
   - Образование

```python
import tika
tika.initVM()
from tika import parser

parsed = parser.from_file('resume.pdf')
text = parsed["content"]
# Дальнейший NLP анализ
```

## **5. Планирование Zoom собеседований**
**Технологии:**
- Zoom API
- Google Calendar API
- Microsoft Graph API (для Outlook)

**Реализация:**
1. Клиент подключает Zoom через OAuth
2. Система получает доступ к созданию встреч
3. Алгоритм:
   - Проверка доступности HR
   - Создание Zoom встречи
   - Добавление в календарь
   - Отправка приглашений

```python
# Создание Zoom встречи
def create_zoom_meeting(topic, start_time, duration):
    headers = {
        "Authorization": f"Bearer {zoom_token}",
        "Content-Type": "application/json"
    }
    data = {
        "topic": topic,
        "type": 2,
        "start_time": start_time,
        "duration": duration,
        "settings": {
            "join_before_host": True
        }
    }
    response = requests.post(
        "https://api.zoom.us/v2/users/me/meetings",
        headers=headers,
        json=data
    )
    return response.json()
```

## **6. Синхронизация с календарями**
### **Google Calendar**
1. OAuth 2.0 авторизация
2. Использование Google Calendar API
3. Автоматическое создание событий

### **Outlook Calendar**
1. Microsoft Graph API
2. Аналогичный подход к Google

## **7. Технический стек**
| Компонент       | Технологии                          |
|----------------|-----------------------------------|
| **Backend**    | Python (FastAPI), Node.js         |
| **Frontend**   | React, Tailwind CSS               |
| **Базы данных**| PostgreSQL, MongoDB               |
| **AI/ML**      | SpaCy, Transformers, Custom Models|
| **Интеграции** | Telegram/Slack/LinkedIn APIs      |
| **Инфраструктура** | Docker, Kubernetes, AWS/GCP    |

## **8. Процесс быстрого подключения**
1. Клиент заходит в раздел "Интеграции"
2. Выбирает платформу (Telegram/Slack/LinkedIn)
3. Следует инструкции (вводит токен/авторизуется)
4. Система тестирует подключение
5. Интеграция активирована

**Пример UX flow:**
```
[Интеграции] → [Slack] → [Авторизация через OAuth] → [Выбор канала] → [Готово]
```

### Техническая реализация блока Analytics
1. Общая архитектура аналитики
Data Sources → ETL Pipeline → Data Warehouse → Analytics Engine → Visualization
       ↑
Real-time Updates
2. Общая воронка найма
Метрики и визуализация
Этапы воронки:

Просмотр вакансии

Отправка резюме

AI-скрининг

Интервью

Оффер

Принятие оффера

Визуализация:

Интерактивная воронка (D3.js или Chart.js)

Конверсия между этапами (%)

Среднее время на этапе

Технологии
Сбор данных:

PostgreSQL для хранения событий

Kafka для real-time событий

Обработка:

Python (Pandas, PySpark)

SQL-запросы для агрегации

Пример SQL:

sql
SELECT 
  COUNT(CASE WHEN status = 'applied' THEN 1 END) as applied,
  COUNT(CASE WHEN status = 'interview' THEN 1 END) as interviewed,
  COUNT(CASE WHEN status = 'hired' THEN 1 END) as hired,
  COUNT(CASE WHEN status = 'applied' THEN 1 END) / 
    NULLIF(COUNT(*), 0) as conversion_rate
FROM candidates
WHERE job_id = '123'
3. Статистика по вакансиям
Ключевые метрики
Время закрытия вакансии (среднее/медиана)

Количество кандидатов на вакансию

Соотношение:

Приглашено/Нанято

Отказы/Принятия офферов

Источники кандидатов (LinkedIn, сайт, рефералы)

Реализация
Дашборд:

Таблицы с сортировкой (React-Table)

Графики временных рядов (за последние 30/90 дней)

Технологии:

Elasticsearch для быстрого поиска

Materialized Views в PostgreSQL для агрегаций

Пример данных:

json
{
  "job_id": "123",
  "title": "Frontend Developer",
  "total_candidates": 45,
  "hired": 1,
  "avg_time_to_hire": "14 days",
  "sources": {
    "linkedin": 30,
    "website": 10,
    "referral": 5
  }
}
4. KPI по эффективности кандидатов и ИИ
Метрики для AI
Точность AI-скрининга:

Сравнение рекомендаций AI с финальными решениями HR

False Positive/Negative rate

Эффективность вопросов:

Какие вопросы лучше выявляют сильные/слабые стороны

Время обработки:

Среднее время анализа резюме

Время проведения интервью

Метрики для кандидатов
Рейтинг кандидата (по оценкам AI и HR)

Сравнение:

Оценка AI vs Реальная производительность (если наняты)

Топ навыков успешных кандидатов

Реализация
ML-модели:

Регрессия для предсказания успешности найма

Кластеризация кандидатов

Инструменты:

Python (scikit-learn, TensorFlow)

Jupyter Notebook для анализа

Пример расчета KPI:

python
from sklearn.metrics import accuracy_score

# y_true - решения HR, y_pred - рекомендации AI
accuracy = accuracy_score(y_true, y_pred)
print(f"AI Accuracy: {accuracy:.2%}")
5. Технический стек
Компонент	Технологии
Хранение данных	PostgreSQL, TimescaleDB для временных рядов
Обработка	Python (Pandas, PySpark), SQL
Real-time	Kafka, Apache Flink
Визуализация	React + D3.js, Tableau Embedded
ML/AI	scikit-learn, TensorFlow, PyTorch
Инфраструктура	Docker, Kubernetes, AWS Redshift
6. Пример дашборда
javascript
// React-компонент графика
import { FunnelChart, Funnel, Tooltip } from 'recharts';

const data = [
  { stage: 'Applied', value: 100 },
  { stage: 'Screened', value: 60 },
  { stage: 'Interviewed', value: 30 },
  { stage: 'Hired', value: 1 }
];

const HiringFunnel = () => (
  <FunnelChart width={500} height={300}>
    <Funnel data={data} dataKey="value"/>
    <Tooltip/>
  </FunnelChart>
);
7. Особенности реализации
Real-time обновления:

WebSocket для мгновенного обновления дашбордов

Кеширование частых запросов (Redis)

Гибкость отчетов:

Пользовательские фильтры (по времени, департаменту и т.д.)

Экспорт в CSV/PDF

Интеграция:

Webhook для отправки отчетов в Slack/Email

API для доступа к данным

Масштабируемость:

Колонковые БД для аналитики (ClickHouse)

Пакетная обработка ночью для тяжелых отчетов

# **Company Profile


**Всего 3 основных раздела:**
1. **Основное о компании** (5 обязательных полей)
2. **Вакансии** (простое добавление за 1 минуту)
3. **Настройки найма** (умные шаблоны)

## **2. Реализация "Основное о компании"**

### **Минимальные поля:**
```markdown
1. [Название компании] ___________
2. [Сфера деятельности] ▾ (выпадающий список из 10 вариантов)
3. [Размер компании] ▾ (1-10, 11-50, 51-200, 201+)
4. [Страна/город] 🌍 (автодополнение)
5. [Логотип] (перетащите файл сюда)
```

**Технологии:**
- **Автокомплит городов**: Google Places API (бесплатный тариф)
- **Загрузка лого**: 
  ```javascript
  // Простой drag-ndrop на React
  const {getRootProps, getInputProps} = useDropzone({
    accept: 'image/*',
    maxFiles: 1,
    onDrop: files => uploadLogo(files[0])
  })
  ```

## **3. Упрощенное добавление вакансий**

### **"Smart-форма" вакансии:**
1. **Название должности** (с подсказками)
   - Пример: Начинаете вводить "Marke..." → "Marketer", "Marketing Manager"
   
2. **Автогенерация описания**:
   - Кнопка "Создать описание с ИИ" (использует GPT-4)
   - Редактирование в простом текстовом поле

3. **Требования**:
   - Чекбоксы популярных навыков (React, Python и т.д.)
   - Поле "Добавить свое" (не более 5 ключевых)

**Бэкенд обработка:**
```python
async def create_job(job_data):
    if not job_data.get('description'):
        # Генерируем описание если не заполнено
        job_data['description'] = ai_generate_description(job_data['title'])
    return save_job(job_data)
```

## **4. Умные шаблоны политик**

**Готовые варианты на выбор:**
- 🟢 Базовый найм (стандартный)
- 🔵 Технический найм (с код-ревью)
- 🟣 Строгий найм (5 этапов собеседований)

**Реализация:**
```javascript
// React компонент выбора шаблона
<TemplatePicker 
  templates={templates}
  onChange={(selected) => setPolicy(selected.defaultRules)}
/>
```

## **5. Технологии для простоты**

| Что упрощаем       | Как реализуем                     |
|--------------------|----------------------------------|
| **Ввод данных**    | Автодополнение, подсказки        |
| **Загрузка файлов**| Drag-n-drop + авторесайз изображений |
| **Сложные поля**   | Пошаговые формы + умолчания      |
| **Интеграции**     | OAuth-авторизация в 1 клик       |

## **6. Пример UI-флоу**

```mermaid
flowchart TB
    A[Старт] --> B{Есть профиль?}
    B -->|Нет| C[5 полей о компании]
    B -->|Да| D[Добавить вакансию]
    C --> D
    D --> E[Выбрать шаблон найма]
    E --> F[Готово!]
```

## **7. Особенности реализации**

1. **Автосохранение**:
   - Все данные сохраняются при потере фокуса с поля
   - Локальное хранилище на случай разрыва соединения

2. **Минимум действий**:
   - При первом входе: "Давайте заполним 3 поля и начнем!"
   - Кнопка "Пропустить" для всех необязательных полей

3. **Визуальные подсказки**:
   ```javascript
   <Field 
     label="Название компании"
     hint="Как вас узнают кандидаты"
     required
   />
   ```

4. **Мобильная версия**:
   - Приоритет основным полям
   - Крупные интерактивные элементы

## **8. Бэкенд для простоты**

**Структура БД:**
```python
class Company(Model):
    name = CharField(max_length=100)
    industry = CharField(choices=INDUSTRIES)
    size = CharField(choices=SIZES)
    # Остальные поля nullable=True
```

**API ответ всегда содержит подсказки:**
```json
{
  "data": {...},
  "next_steps": [
    {"action": "add_logo", "priority": 2},
    {"action": "create_first_job", "priority": 1}
  ]
}
```

# **Billing блок

## **1. Общая архитектура**
```
Frontend (React) ↔ Backend API ↔ Payment Processor ↔ Accounting Tools
       ↑               ↑
Subscription Manager   Database
```

## **2. План подписки**

### **Модель подписок**
- **Типы планов**:
  ```javascript
  const PLANS = [
    {
      id: 'basic',
      name: 'Basic',
      price: '$29/мес',
      limits: '10 вакансий/мес',
      features: ['AI анализ резюме', '5 интервью/мес']
    },
    {
      id: 'pro',
      name: 'Pro',
      price: '$99/мес', 
      limits: '50 вакансий/мес',
      features: ['Все в Basic +', 'Zoom интеграция', 'Приоритетная поддержка']
    }
  ]
  ```

### **Технологии реализации**
- **Frontend**:
  - React-компоненты с переключением тарифов
  - Анимация при выборе плана (Framer Motion)
  ```jsx
  <PlanCard 
    plan={plan}
    selected={selectedPlan === plan.id}
    onClick={() => setSelectedPlan(plan.id)}
  />
  ```

- **Backend**:
  - Модель подписок в БД:
  ```python
  class SubscriptionPlan(BaseModel):
      id: str
      name: str
      price: int  # в центах
      currency: str = "USD"
      features: List[str]
  ```

## **3. История оплат**

### **Структура данных**
```json
{
  "id": "txn_123",
  "date": "2023-10-01T14:30:00Z",
  "amount": 2900,
  "currency": "USD",
  "plan": "basic",
  "status": "completed",
  "invoice_url": "https://.../invoice_123.pdf"
}
```

### **Реализация**
- **Графический интерфейс**:
  - Фильтры по дате/статусу
  - Экспорт в CSV
  ```javascript
  <PaymentsTable
    data={payments}
    onFilterChange={(filters) => fetchPayments(filters)}
  />
  ```

- **Бэкенд**:
  ```python
  @app.get("/payments")
  async def get_payments(user: User, start_date: str = None):
      query = PaymentHistory.where(user_id=user.id)
      if start_date:
          query = query.filter(created_at >= start_date)
      return await query
  ```

## **4. Выставление счетов**

### **Автоматизированный процесс**
1. **Генерация счета**:
   - Использование шаблонов (HTML → PDF)
   - Нумерация (INV-2023-10-001)
2. **Доставка**:
   - Email (SendGrid)
   - Доступ в личном кабинете
3. **Оплата**:
   - Ссылка на платежную систему
   - Автоматическая проверка оплаты

### **Технологии**
- **PDF генерация**:
  ```python
  from weasyprint import HTML
  HTML(string=invoice_html).write_pdf("invoice.pdf")
  ```
  
- **Интеграции**:
  - **Stripe** для платежей
  - **QuickBooks** (API) для бухгалтерии

## **5. Полный технический стек**

| Компонент            | Технологии                          |
|----------------------|------------------------------------|
| **Платежная система**| Stripe / PayPal / Paddle           |
| **PDF генерация**    | WeasyPrint / PDFKit                |
| **Бэкенд**           | FastAPI (Python) / NestJS (Node.js)|
| **Frontend**         | React + Stripe Elements            |
| **База данных**      | PostgreSQL (транзакции)            |
| **Email**            | SendGrid / Postmark                |
| **Хранение счетов**  | S3 + CloudFront                    |

## **6. Пример API эндпоинтов**

```python
# Смена подписки
@app.post("/subscription/upgrade")
async def upgrade_subscription(
    user: User,
    plan_id: str,
    payment_method: str
):
    # Проверка доступа
    if not user.can_upgrade:
        raise HTTPException(403, "Недостаточно средств")
    
    # Списание средств
    charge = stripe.Charge.create(
        amount=PLANS[plan_id].price,
        currency="usd",
        source=payment_method
    )
    
    # Обновление подписки
    user.subscription = plan_id
    await user.save()
    
    # Отправка чека
    send_receipt_email(user.email, charge.receipt_url)
    
    return {"status": "success"}
```

## **7. Особенности реализации**

1. **Тестовый режим**:
   - Sandbox Stripe для тестирования
   - Фейковые платежи в dev-среде

2. **Безопасность**:
   - Отдельная таблица для платежных данных
   - Audit log всех операций

3. **Уведомления**:
   - Webhook от Stripe о платежах
   - Напоминания об окончании подписки

4. **Локализация**:
   - Поддержка валют (USD, EUR, RUB)
   - Локализованные шаблоны счетов

5. **Отказоустойчивость**:
   - Повторные попытки при неудачных платежах
   - Ручное продление при проблемах с оплатой

## **8. Процесс оплаты (UI Flow)**

```mermaid
sequenceDiagram
    Пользователь->>Frontend: Выбирает план
    Frontend->>Backend: Запрос на создание подписки
    Backend->>Stripe: Создает Payment Intent
    Stripe-->>Backend: Возвращает client_secret
    Backend-->>Frontend: Возвращает данные для оплаты
    Frontend->>Stripe JS: Подтверждение платежа
    Stripe JS-->>Frontend: Результат оплаты
    Frontend->>Backend: Подтверждение
    Backend->>БД: Активирует подписку
    Backend->>Пользователь: Отправляет счет
```

## **9. Мониторинг и аналитика**

- **Дашборд** с ключевыми метриками:
  - MRR (Monthly Recurring Revenue)
  - Churn rate
  - Средний чек

- **Интеграция**:
  - Stripe → Metabase
  - Google Analytics для трекинга конверсий



# **Техническая реализация блока Integrations**

## **1. Общая архитектура интеграций**
```
Frontend (React) ↔ Integration Gateway ↔ Внешние системы (ATS/HRMS)
       ↑               ↑
API Key Management    Webhook Processor
```

## **2. Подключение к ATS/HRMS (BambooHR, Greenhouse)**

### **Технологический стек**
| Система       | Технологии интеграции              | Особенности                     |
|--------------|-----------------------------------|--------------------------------|
| **BambooHR** | OAuth 2.0 + REST API              | Требует верификацию приложения |
| **Greenhouse** | API Key + Webhooks               | Богатый API для кандидатов     |
| **Workable** | OAuth + GraphQL API               | Сложная схема аутентификации   |

### **Реализация**
**Backend (Python пример):**
```python
# BambooHR OAuth flow
@app.get("/integrations/bamboohr/connect")
async def connect_bamboohr():
    redirect_uri = "https://yourdomain.com/auth/callback"
    return RedirectResponse(
        f"https://bamboohr.com/authorize?client_id=YOUR_ID&redirect_uri={redirect_uri}"
    )

@app.get("/integrations/bamboohr/callback")
async def bamboohr_callback(code: str):
    token = get_bamboohr_token(code)
    save_integration("bamboohr", user.id, token)
    return {"status": "connected"}
```

**Frontend компонент:**
```jsx
<IntegrationCard
  title="BambooHR"
  icon="bamboohr-logo.svg"
  description="Синхронизация кандидатов"
  onConnect={() => startOAuthFlow('bamboohr')}
  connected={isConnected('bamboohr')}
/>
```

## **3. Webhooks/API система**

### **Архитектура обработки вебхуков**
```
Внешняя система → Webhook Gateway → Event Processor → DB/Notification
```

### **Ключевые компоненты**
1. **Webhook Gateway**:
   - Валидация подписей
   - Роутинг событий
   - Логирование

2. **Event Processor**:
   - Очередь задач (Redis + Celery)
   - Обработчики для разных типов событий

**Пример кода (Node.js):**
```javascript
// Webhook endpoint
app.post('/webhooks/:provider', async (req, res) => {
  const provider = req.params.provider;
  const event = validateWebhook(provider, req);
  
  await processEventQueue.add({ 
    provider, 
    event,
    userId: req.user.id
  });

  res.status(200).send('OK');
});

// Обработчик
processEventQueue.process(async (job) => {
  const { provider, event } = job.data;
  switch (event.type) {
    case 'candidate.created':
      await handleNewCandidate(provider, event.data);
      break;
    // Другие типы событий...
  }
});
```

## **4. Виджет для сайта**

### **Технологии реализации**
- **Виджет резюме**:
  - Web Component (для максимальной совместимости)
  - IFrame вариант (как fallback)

**Пример Web Component:**
```javascript
class RappleWidget extends HTMLElement {
  constructor() {
    super();
    this.attachShadow({ mode: 'open' });
    this.shadowRoot.innerHTML = `
      <style>/* CSS стили */</style>
      <div class="widget-container">
        <form id="upload-form">
          <input type="file" accept=".pdf,.docx">
          <button type="submit">Отправить резюме</button>
        </form>
      </div>
    `;
  }
}
customElements.define('rapple-widget', RappleWidget);
```

### **Параметры подключения**
```html
<script src="https://cdn.rapple.com/widget/v1.js"></script>
<rapple-widget 
  company="your-company-id"
  vacancy="senior-developer"
  theme="dark"
></rapple-widget>
```

## **5. Полный технический стек**

| Компонент            | Технологии                          |
|----------------------|------------------------------------|
| **OAuth провайдеры** | Auth0, Okta, собственный сервис    |
| **API Gateway**      | Kong, Apigee                       |
| **Очереди событий**  | RabbitMQ, AWS SQS                  |
| **Виджеты**         | Web Components, IFrame             |
| **Мониторинг**      | Prometheus + Grafana               |
| **Логирование**     | ELK Stack (Elasticsearch, Logstash, Kibana) |

## **6. Процесс подключения интеграции**

```mermaid
sequenceDiagram
    Пользователь->>Frontend: Выбирает интеграцию
    Frontend->>Backend: Запрос на подключение
    Backend->>Внешняя система: OAuth запрос
    Внешняя система-->>Пользователь: Запрос разрешений
    Пользователь->>Внешняя система: Подтверждает доступ
    Внешняя система-->>Backend: Auth code
    Backend->>Внешняя система: Запрашивает токен
    Внешняя система-->>Backend: Возвращает токен
    Backend->>БД: Сохраняет интеграцию
    Backend-->>Frontend: Подтверждение успеха
```

## **7. Особенности реализации**

1. **Безопасность**:
   - Шифрование учетных данных интеграций (AWS KMS)
   - Валидация вебхуков через HMAC
   - Ротация API ключей

2. **Мониторинг**:
   - Дашборд статуса интеграций
   - Уведомления о проблемах (Slack, Email)

3. **Управление ошибками**:
   - Автоматический ретрай при сбоях
   - Логирование всех синхронизаций

4. **Производительность**:
   - Кеширование часто запрашиваемых данных
   - Фоновые задания для тяжелых операций

5. **UX улучшения**:
   - Мастер подключения "step-by-step"
   - Тестовый режим для проверки интеграции
   - Визуализация потока данных

## **8. Пример API для управления интеграциями**

```python
# Получение списка доступных интеграций
@app.get("/integrations")
async def list_integrations():
    return [
        {
            "id": "bamboohr",
            "name": "BambooHR",
            "logo": "/logos/bamboohr.png",
            "scopes": ["read_candidates", "write_jobs"]
        },
        # ... другие интеграции
    ]

# Проверка статуса подключения
@app.get("/integrations/{provider}/status")
async def check_status(provider: str, user: User):
    integration = get_integration(provider, user.id)
    return {
        "connected": bool(integration),
        "last_sync": integration.last_sync if integration else None
    }
```



# **Техническая реализация блока Integrations**

## **1. Общая архитектура интеграций**
```
Frontend (React) ↔ Integration Gateway ↔ Внешние системы (ATS/HRMS)
       ↑               ↑
API Key Management    Webhook Processor
```

## **2. Подключение к ATS/HRMS (BambooHR, Greenhouse)**

### **Технологический стек**
| Система       | Технологии интеграции              | Особенности                     |
|--------------|-----------------------------------|--------------------------------|
| **BambooHR** | OAuth 2.0 + REST API              | Требует верификацию приложения |
| **Greenhouse** | API Key + Webhooks               | Богатый API для кандидатов     |
| **Workable** | OAuth + GraphQL API               | Сложная схема аутентификации   |

### **Реализация**
**Backend (Python пример):**
```python
# BambooHR OAuth flow
@app.get("/integrations/bamboohr/connect")
async def connect_bamboohr():
    redirect_uri = "https://yourdomain.com/auth/callback"
    return RedirectResponse(
        f"https://bamboohr.com/authorize?client_id=YOUR_ID&redirect_uri={redirect_uri}"
    )

@app.get("/integrations/bamboohr/callback")
async def bamboohr_callback(code: str):
    token = get_bamboohr_token(code)
    save_integration("bamboohr", user.id, token)
    return {"status": "connected"}
```

**Frontend компонент:**
```jsx
<IntegrationCard
  title="BambooHR"
  icon="bamboohr-logo.svg"
  description="Синхронизация кандидатов"
  onConnect={() => startOAuthFlow('bamboohr')}
  connected={isConnected('bamboohr')}
/>
```

## **3. Webhooks/API система**

### **Архитектура обработки вебхуков**
```
Внешняя система → Webhook Gateway → Event Processor → DB/Notification
```

### **Ключевые компоненты**
1. **Webhook Gateway**:
   - Валидация подписей
   - Роутинг событий
   - Логирование

2. **Event Processor**:
   - Очередь задач (Redis + Celery)
   - Обработчики для разных типов событий

**Пример кода (Node.js):**
```javascript
// Webhook endpoint
app.post('/webhooks/:provider', async (req, res) => {
  const provider = req.params.provider;
  const event = validateWebhook(provider, req);
  
  await processEventQueue.add({ 
    provider, 
    event,
    userId: req.user.id
  });

  res.status(200).send('OK');
});

// Обработчик
processEventQueue.process(async (job) => {
  const { provider, event } = job.data;
  switch (event.type) {
    case 'candidate.created':
      await handleNewCandidate(provider, event.data);
      break;
    // Другие типы событий...
  }
});
```

## **4. Виджет для сайта**

### **Технологии реализации**
- **Виджет резюме**:
  - Web Component (для максимальной совместимости)
  - IFrame вариант (как fallback)

**Пример Web Component:**
```javascript
class RappleWidget extends HTMLElement {
  constructor() {
    super();
    this.attachShadow({ mode: 'open' });
    this.shadowRoot.innerHTML = `
      <style>/* CSS стили */</style>
      <div class="widget-container">
        <form id="upload-form">
          <input type="file" accept=".pdf,.docx">
          <button type="submit">Отправить резюме</button>
        </form>
      </div>
    `;
  }
}
customElements.define('rapple-widget', RappleWidget);
```

### **Параметры подключения**
```html
<script src="https://cdn.raplle.com/widget/v1.js"></script>
<raplle-widget 
  company="your-company-id"
  vacancy="senior-developer"
  theme="dark"
></raplle-widget>
```

## **5. Полный технический стек**

| Компонент            | Технологии                          |
|----------------------|------------------------------------|
| **OAuth провайдеры** | Auth0, Okta, собственный сервис    |
| **API Gateway**      | Kong, Apigee                       |
| **Очереди событий**  | RabbitMQ, AWS SQS                  |
| **Виджеты**         | Web Components, IFrame             |
| **Мониторинг**      | Prometheus + Grafana               |
| **Логирование**     | ELK Stack (Elasticsearch, Logstash, Kibana) |

## **6. Процесс подключения интеграции**

```mermaid
sequenceDiagram
    Пользователь->>Frontend: Выбирает интеграцию
    Frontend->>Backend: Запрос на подключение
    Backend->>Внешняя система: OAuth запрос
    Внешняя система-->>Пользователь: Запрос разрешений
    Пользователь->>Внешняя система: Подтверждает доступ
    Внешняя система-->>Backend: Auth code
    Backend->>Внешняя система: Запрашивает токен
    Внешняя система-->>Backend: Возвращает токен
    Backend->>БД: Сохраняет интеграцию
    Backend-->>Frontend: Подтверждение успеха
```

## **7. Особенности реализации**

1. **Безопасность**:
   - Шифрование учетных данных интеграций (AWS KMS)
   - Валидация вебхуков через HMAC
   - Ротация API ключей

2. **Мониторинг**:
   - Дашборд статуса интеграций
   - Уведомления о проблемах (Slack, Email)

3. **Управление ошибками**:
   - Автоматический ретрай при сбоях
   - Логирование всех синхронизаций

4. **Производительность**:
   - Кеширование часто запрашиваемых данных
   - Фоновые задания для тяжелых операций

5. **UX улучшения**:
   - Мастер подключения "step-by-step"
   - Тестовый режим для проверки интеграции
   - Визуализация потока данных

## **8. Пример API для управления интеграциями**

```python
# Получение списка доступных интеграций
@app.get("/integrations")
async def list_integrations():
    return [
        {
            "id": "bamboohr",
            "name": "BambooHR",
            "logo": "/logos/bamboohr.png",
            "scopes": ["read_candidates", "write_jobs"]
        },
        # ... другие интеграции
    ]

# Проверка статуса подключения
@app.get("/integrations/{provider}/status")
async def check_status(provider: str, user: User):
    integration = get_integration(provider, user.id)
    return {
        "connected": bool(integration),
        "last_sync": integration.last_sync if integration else None
    }
```



# **Техническая реализация блока Profile Settings**

## **1. Общая архитектура**
```
Frontend (React) ↔ Backend API (Node.js/FastAPI) ↔ Database
       ↑
Real-time Updates (WebSockets)
```

## **2. Данные пользователя**

### **Структура данных**
```typescript
interface UserProfile {
  id: string;
  email: string;
  personalInfo: {
    firstName: string;
    lastName: string;
    avatarUrl?: string;
    timezone: string;
    language: 'en' | 'ru' | 'es';
  };
  companyInfo?: {
    position: string;
    department: string;
  };
}
```

### **Технологии реализации**
- **Форма редактирования**:
  - React Hook Form для валидации
  - Drag-and-drop загрузка аватара (используя react-dropzone)
  
```jsx
<Form onSubmit={handleSubmit}>
  <AvatarUploader 
    onDrop={(files) => uploadAvatar(files[0])}
    currentAvatar={user.avatarUrl}
  />
  <Input 
    name="firstName"
    label="Имя"
    defaultValue={user.firstName}
  />
  <TimezoneSelect 
    value={user.timezone}
    onChange={(tz) => updateField('timezone', tz)}
  />
</Form>
```

- **Бэкенд обработка**:
```python
@app.patch("/users/me")
async def update_user_profile(
    user: User = Depends(current_user),
    update_data: UserProfileUpdate = Body(...)
):
    updated_user = await user.update(update_data)
    return JSONResponse(updated_user.dict())
```

## **3. Настройки безопасности**

### **Ключевые функции**
1. **Смена пароля**:
   - Требование сложности (zxcvbn библиотека)
   - Подтверждение по email

2. **Двухфакторная аутентификация**:
   - TOTP (Google Authenticator)
   - Резервные коды

3. **Активные сессии**:
   - Просмотр и завершение
   - Геолокация по IP

### **Реализация**
**Frontend компоненты**:
```jsx
<SecuritySettings>
  <PasswordForm 
    onPasswordChange={handlePasswordChange}
    complexityRules={[
      { label: '8+ символов', validator: (p) => p.length >= 8 },
      { label: 'Цифры', validator: (p) => /\d/.test(p) }
    ]}
  />
  
  <TwoFactorAuth 
    enabled={user.2fa_enabled}
    onToggle={toggle2FA}
  />
</SecuritySettings>
```

**Backend логика 2FA**:
```python
@app.post("/users/me/2fa/enable")
async def enable_2fa(user: User):
    secret = pyotp.random_base32()
    user.totp_secret = secret
    await user.save()
    
    # Генерация QR кода
    provisioning_uri = pyotp.totp.TOTP(secret).provisioning_uri(
        name=user.email,
        issuer_name="Raplle"
    )
    qr_code = qrcode.make(provisioning_uri)
    
    return {
        "secret": secret,
        "qr_code": qr_code_to_base64(qr_code)
    }
```

## **4. Управление уведомлениями**

### **Система уведомлений**
```typescript
interface NotificationSettings {
  email: {
    newCandidate: boolean;
    interviewReminder: boolean;
    weeklyDigest: boolean;
  };
  push: {
    immediateAlerts: boolean;
    mentionNotifications: boolean;
  };
  slack?: {
    channel?: string;
    notifyOn: string[];
  };
}
```

### **Технологии**
- **WebSocket для реальных уведомлений**:
  ```javascript
  const socket = new WebSocket(`wss://api.raplle.com/notifications?token=${userToken}`);
  
  socket.onmessage = (event) => {
    const notification = JSON.parse(event.data);
    showToast(notification);
  };
  ```

- **Настройки компонента**:
```jsx
<NotificationPreferences>
  <Toggle
    label="Email: Новые кандидаты"
    checked={notifications.email.newCandidate}
    onChange={(v) => updateNotificationPref('email.newCandidate', v)}
  />
  <ChannelSelect
    channels={slackChannels}
    selected={notifications.slack?.channel}
    onSelect={(c) => updateSlackChannel(c)}
  />
</NotificationPreferences>
```

## **5. Полный технический стек**

| Компонент               | Технологии                          |
|-------------------------|------------------------------------|
| **Frontend**           | React, Tailwind CSS, Framer Motion |
| **Формы**              | React Hook Form, Formik            |
| **Загрузка файлов**    | AWS S3 Presigned URLs              |
| **Аутентификация**     | JWT, OAuth 2.0                     |
| **2FA**                | PyOTP, Speakeasy                   |
| **Уведомления**        | WebSockets, Firebase Cloud Messaging |
| **Бэкенд**             | FastAPI (Python), NestJS (Node.js) |
| **База данных**        | PostgreSQL (для пользователей), Redis (сессии) |

## **6. Процесс обновления профиля**

```mermaid
sequenceDiagram
    Пользователь->>Frontend: Вносит изменения
    Frontend->>Backend: PATCH /users/me
    Backend->>Database: Обновляет запись
    Database-->>Backend: Подтверждение
    Backend->>Redis: Инвалидирует кеш
    Backend-->>Frontend: Успешный ответ
    Frontend->>WebSocket: Запрос новых данных
    WebSocket-->>Frontend: Обновлённый профиль
```

## **7. Особые фичи**

1. **Аватар AI**:
   - Генерация граватара на основе email
   - AI-генерация по описанию (используя DALL-E мини)

2. **Аудит безопасности**:
   - Оценка надежности пароля
   - История изменений критичных полей

3. **Темная тема**:
   - Сохранение предпочтений в профиле
   - Sync между устройствами через API

4. **Экспорт данных**:
   - GDPR-совместимый экспорт всех данных
   ```python
   @app.get("/users/me/export")
   async def export_user_data(user: User):
       data = prepare_gdpr_export(user)
       return StreamingResponse(
           generate_zip(data),
           media_type="application/zip"
       )
   ```

## **8. Оптимизации**

1. **Дебаунс обновлений**:
   ```javascript
   // Оптимизация частых запросов
   const debouncedSave = useDebouncedCallback(
     (data) => saveProfile(data),
     1000
   );
   ```

2. **Оптимистичные обновления**:
   ```jsx
   const [avatar, setAvatar] = useState(user.avatarUrl);
   
   const handleUpload = async (file) => {
     const tempUrl = URL.createObjectURL(file);
     setAvatar(tempUrl); // Оптимистичное обновление
     
     try {
       await api.uploadAvatar(file);
     } catch (error) {
       setAvatar(user.avatarUrl); // Откат при ошибке
     }
   }
   ```

3. **Кеширование**:
   - SWR/stale-while-revalidate для данных профиля
   - Redis кеш на бэкенде



# **Техническая реализация навигационной системы**

## **1. Архитектура навигации**
```
Навигационный контейнер → Динамические пункты меню → Состояние активного элемента
       ↑
Конфигурация прав доступа
```

## **2. Технологический стек**

| Компонент               | Технологии                          | Назначение                     |
|-------------------------|------------------------------------|-------------------------------|
| **Базовый компонент**   | React + TypeScript                 | Ядро навигации                |
| **Стили**              | Tailwind CSS + Headless UI         | Адаптивный дизайн             |
| **Состояние**          | Zustand / Jotai                    | Управление активным пунктом   |
| **Роутинг**            | React Router 6                     | Навигация между разделами     |
| **Анимации**           | Framer Motion                      | Плавные переходы              |
| **Адаптивность**       | useMediaQuery                      | Переключение Sidebar/Topbar   |

## **3. Реализация Sidebar**

### **Компонент Sidebar**
```tsx
import { useStore } from '@/store/navigation';

export const Sidebar = () => {
  const { activeTab, setActiveTab } = useStore();
  const isMobile = useMediaQuery('(max-width: 768px)');

  return (
    <nav className={`bg-white dark:bg-gray-800 ${isMobile ? 'w-full' : 'w-64'}`}>
      <div className="flex flex-col space-y-1 p-4">
        {NAV_ITEMS.map((item) => (
          <NavItem 
            key={item.path}
            icon={item.icon}
            label={item.label}
            active={activeTab === item.path}
            onClick={() => setActiveTab(item.path)}
          />
        ))}
      </div>
    </nav>
  );
};
```

### **Анимированный NavItem**
```tsx
const NavItem = ({ icon, label, active, onClick }) => (
  <motion.div 
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
  >
    <button
      className={`flex items-center p-3 rounded-lg transition-colors ${
        active ? 'bg-blue-100 text-blue-600' : 'hover:bg-gray-100'
      }`}
      onClick={onClick}
    >
      <IconComponent name={icon} className="mr-3" />
      <span>{label}</span>
    </button>
  </motion.div>
);
```

## **4. Реализация Topbar (для мобильных)**

```tsx
export const Topbar = () => {
  const { activeTab } = useStore();
  
  return (
    <header className="fixed top-0 left-0 right-0 bg-white shadow-sm">
      <div className="flex overflow-x-auto py-2 px-4 hide-scrollbar">
        {NAV_ITEMS.map((item) => (
          <TopbarItem 
            key={item.path}
            item={item}
            active={activeTab === item.path}
          />
        ))}
      </div>
    </header>
  );
};

const TopbarItem = ({ item, active }) => (
  <div className={`px-3 py-2 mx-1 rounded-full whitespace-nowrap ${
    active ? 'bg-blue-500 text-white' : 'bg-gray-100'
  }`}>
    <Link to={item.path}>
      <div className="flex items-center">
        <IconComponent name={item.icon} size={18} className="mr-2" />
        <span className="text-sm">{item.label}</span>
      </div>
    </Link>
  </div>
);
```

## **5. Конфигурация навигации**

```ts
export const NAV_ITEMS = [
  {
    path: '/dashboard',
    label: 'Dashboard',
    icon: 'dashboard',
    roles: ['admin', 'hr', 'manager']
  },
  {
    path: '/resume-analyzer',
    label: 'Resume Analyzer',
    icon: 'document-search',
    roles: ['admin', 'hr']
  },
  // ... другие пункты
] as const;

export type NavigationPath = typeof NAV_ITEMS[number]['path'];
```

## **6. Управление состоянием**

```ts
import { create } from 'zustand';

interface NavigationState {
  activeTab: NavigationPath;
  setActiveTab: (path: NavigationPath) => void;
  mobileMenuOpen: boolean;
  toggleMobileMenu: () => void;
}

export const useNavigationStore = create<NavigationState>((set) => ({
  activeTab: '/dashboard',
  setActiveTab: (path) => set({ activeTab: path }),
  mobileMenuOpen: false,
  toggleMobileMenu: () => set((state) => ({ mobileMenuOpen: !state.mobileMenuOpen })),
}));
```

## **7. Адаптивная логика**

```tsx
export const NavigationProvider = () => {
  const isDesktop = useMediaQuery('(min-width: 1024px)');
  const { mobileMenuOpen, toggleMobileMenu } = useNavigationStore();

  return (
    <>
      {isDesktop ? (
        <Sidebar />
      ) : (
        <>
          <Topbar />
          <MobileMenuButton onClick={toggleMobileMenu} />
          <MobileMenu open={mobileMenuOpen} />
        </>
      )}
    </>
  );
};
```

## **8. Интеграция с роутингом**

```tsx
const AppRouter = () => {
  const { activeTab, setActiveTab } = useNavigationStore();

  return (
    <BrowserRouter>
      <NavigationProvider />
      <Routes>
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/resume-analyzer" element={<ResumeAnalyzer />} />
        {/* ... другие маршруты */}
      </Routes>
    </BrowserRouter>
  );
};
```

## **9. Особые фичи**

1. **Персонализация**:
   ```ts
   // Фильтрация по ролям
   const filteredItems = NAV_ITEMS.filter((item) => 
     user.roles.some(role => item.roles.includes(role))
   );
   ```

2. **Бейджи уведомлений**:
   ```tsx
   <NavItem 
     {...item}
     badge={item.path === '/analytics' ? unreadReports : undefined}
   />
   ```

3. **Кастомные иконки**:
   ```tsx
   const IconComponent = ({ name }: { name: IconName }) => {
     const icons = {
       dashboard: <DashboardIcon />,
       'document-search': <DocumentSearchIcon />,
       // ...
     };
     return icons[name] || null;
   };
   ```

4. **Темная тема**:
   ```tsx
   <nav className={`bg-white dark:bg-gray-800 ${
     active ? 'dark:bg-gray-700' : 'dark:hover:bg-gray-700'
   }`}>
   ```

## **10. Оптимизации производительности**

1. **Динамический импорт**:
   ```tsx
   const Dashboard = React.lazy(() => import('./Dashboard'));
   ```

2. **Виртуализация** (для длинных списков):
   ```tsx
   <FixedSizeList height={600} itemSize={50} itemCount={NAV_ITEMS.length}>
     {({ index, style }) => (
       <div style={style}>
         <NavItem item={NAV_ITEMS[index]} />
       </div>
     )}
   </FixedSizeList>
   ```

3. **Прелоадер** для иконок:
   ```ts
   import { preloadIcons } from './icons';
   
   preloadIcons(['dashboard', 'document-search', /* ... */]);
   ```

